# Record-level MLS caches — DO NOT publish (MLS data licensing). Only derived
# aggregates ever get committed. _raw_band is update-town-band.py's pull cache.
data/_raw_nh/
data/_raw_band/
# secrets
.env

__pycache__/

# local tooling scratch — dev-server config, agent workspace
.claude/
.superpowers/

data/_raw_cbp/
